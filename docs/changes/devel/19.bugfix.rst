**DSS**:
- Fix DSS pattern normalization to preserve physical signal scaling in `compute_dss` and `iterative_dss`, and added `get_normalized_patterns()` to `DSS` and `IterativeDSS` for standard visualization (by [@Antigravity](https://github.com/google-deepmind)).
››