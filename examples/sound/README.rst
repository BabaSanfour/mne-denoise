SOUND example
=============

This example uses the MNE Sample EEG recording and its public forward
solution to show how forward-model information can estimate channel-specific
noise. Known planted corruption is evaluated alongside change on untouched
channels, so the tutorial keeps attenuation and preservation as separate
endpoints.
