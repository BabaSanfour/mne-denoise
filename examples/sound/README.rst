SOUND example
=============

This example uses the MNE Sample EEG recording and its public forward solution
to show how forward-model information can estimate channel-specific noise.
Known planted corruption provides a recovery endpoint, while a separate SOUND
pass on the unmodified recording provides a preservation control.
